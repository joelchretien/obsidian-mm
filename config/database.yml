default: &default
  host: localhost
  adapter: postgresql
  encoding: utf8
  pool: 5
  reconnect: true
  username: <%= ENV['BUDGET_TRACKING_DATABASE_USERNAME'] %>
  password: <%= ENV['BUDGET_TRACKING_DATABASE_PASSWORD'] %>

development:
  <<: *default
  database: budget-tracking_development

test:
  <<: *default
  database: budget-tracking_test

production:
  <<: *default
  database: budget-tracking_production
